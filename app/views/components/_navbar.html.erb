<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Blog app</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to 'Home', '/', class: "nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to 'All posts', '/', class: "nav-link active" %>
        </li>
      </ul>
      <span class="d-grid gap-2 d-md-flex justify-content-md-end">
        <% if user_signed_in? %>
          <%= current_user.name %> /
          <% button_to "Sign out", destroy_user_session_path, class: "btn btn-secondary" %>
        <% else %>
          <%= button_to "Sign in", new_user_session_path, class: "btn btn-primary me-md-1" %>
          <%= link_to "Sign up", new_user_registration_path, class: "btn bg-success text-white" %>
        <% end %>
      </span>
    </div>
  </div>
</nav>
